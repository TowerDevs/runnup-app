/**
 * Metrics constants
 * @module
 */

export enum METRIC_TYPES {
  TIME = "time",
  NUMBER = "number",
}

export enum METRICS {
  DISTANCE = "distance",
  ELEVATION = "elevation",
  PACE = "pace",
  DURATION = "duration",
  CALORIES = "calories"
}
