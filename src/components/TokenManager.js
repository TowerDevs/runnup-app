/**
 * TokenManager component
 * @module TokenManager
 */

/**
 * TokenManager is a higher-level component that stores and provides the auth token.
 * @param {object} props 
 */
export default function TokenManager(props) {

}